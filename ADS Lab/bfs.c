#include <stdio.h>
#include <stdlib.h>

int main()
{
}

void createGraph(int graph[][], int size)
{
    int edges;
    printf("How many edges are there? :");
    scanf("%d", &edges);

    for (int i = 0; i < edges; i++)
    {
        printf("Edge %d");
    }
}